For install the project do you need:
 - composer and php 8
 - ```git clone https://github.com/Lucasg-moreira/pdo.git``` (or ssh if do you prefer)
 - cd into the dir called "pdo"
 - run ```composer install```
 - now just run the scripts php.